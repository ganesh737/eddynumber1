name: Build macOS Release

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build-mac:
    runs-on: macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6 # Updated to avoid Node.js 20 deprecation warning

      - name: Setup Node.js
        uses: actions/setup-node@v6 # Updated to avoid Node.js 20 deprecation warning
        with:
          node-version: 24
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Sync Shader Sources
        run: node scripts/sync-shader-sources.mjs

      - name: Build macOS App
        run: npm run desktop:dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Mac DMG Artifact
        uses: actions/upload-artifact@v4
        with:
          name: macOS-Installer
          path: |
            dist/*.dmg
            dist/*.zip